# ⚠️ MIGRACIONES LEGACY — NO EJECUTAR

Esta carpeta contiene copias de los scripts de `sql/` renombradas como migraciones.
Son duplicados históricos y pueden estar desactualizadas respecto al esquema actual.

**La fuente de verdad son las migraciones con timestamp en `supabase/migrations/`.**

No ejecutes nada de esta carpeta.
